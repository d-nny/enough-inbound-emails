{
    "name": "enough-inbound-emails",
    "version": "1.0.0",
    "description": "Cloudflare Worker for receiving incoming emails",
    "main": "src/index.js",
    "scripts": {
      "dev": "wrangler dev",
      "deploy": "wrangler deploy",
      "test": "echo \"No tests specified\" && exit 0"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
      "wrangler": "^3.0.0"
    }
  }